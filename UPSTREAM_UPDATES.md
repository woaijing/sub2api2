# FoxCode 分支的选择性上游更新

## 2026-09-18：后端修复与密钥提供商选择

从 FoxCode 定制版本 `25ab513f0`（上游业务基线 `871e89c7b` / `0.1.326`）更新。
本次核对的上游 main 为 `bbdd21d98f30fbbce8e3566ae4a4d5b138798acc`。
源码版本随所选功能提交更新为 `0.1.327`。

### 引入的提交

按上游顺序使用 `git cherry-pick -x`，保留来源记录：

| 上游提交 | 内容 |
| --- | --- |
| `fe6bf3226` | Gemini 桥接协议与独立用量统计 |
| `358f2c32f` | 智能路由策略与候选模型上下文 |
| `f56a34fd0` | 路由余额预占与订阅资格绑定 |
| `40198a6dc` | 候选路由的 Messages 分发权限 |
| `e6eaf0836` | Gemini 流式输出预占与取消处理 |
| `78f76ffae` | 余额资格判断计入当前路由预占 |
| `0903f2e91` | 保留 Gemini 拒绝请求用量并避免错误计费 |
| `021958d63` | 模型目录与调度复用候选渠道映射 |
| `25f896f71` | 实际选中路由的计费与转发状态绑定 |
| `4bb0ed006` | 密钥提供商选择及版本号 `0.1.327` |
| `0addf419f` | WebSocket 同线程抢占测试的同步修复 |

### FoxCode 适配范围

密钥创建与编辑支持 Anthropic、OpenAI、国产模型、其他四类提供商筛选。
切换类别会清除失效的单分组选择；智能路由保留已有的跨提供商候选顺序。
没有可用分组的类别禁用。

提供商卡片的选中、悬停、键盘焦点与勾选标记使用 FoxCode 主题颜色，提交按钮继续使用 `btn-primary`。
前端变更仅限密钥页、对应测试及中英文新增文案。
主题变量、全局 CSS、布局、首页、登录注册页、图表和模型广场保持 `25ab513f0` 的实现。

没有引入上游 `0.1.328` 的 Signal 控制台改版、预览功能、部署脚本和发布流水线变更。
后端除版本号外与本次核对的上游 main 一致；没有数据库迁移或依赖版本变更。
随提交带入的 `docs/smart-route-audit-20260917.md` 和 `docs/release-audit-0.1.327.md`
是上游记录，不代表本分支已部署或完成线上验证。

### 本地验证

- 前端全量 ESLint 通过。
- `vue-tsc -b` 与 Vite 生产构建通过。
- 10 个前端测试文件、97 项测试通过，包含密钥提供商、国际化、FoxCode 首页、侧栏、模型广场和图表。
- Go 1.26.6 下智能路由、计费、Gemini 桥接与处理器相关定向测试通过。
- Go 1.26.6 下完整 `internal/service` 和 `internal/handler` 单元测试通过（使用 `-tags=unit`）。
- Go 1.26.6 下 `go build -tags=embed -trimpath` 通过，生成包含本次 FoxCode 前端产物的 Windows 本地程序 `backend/bin/sub2api-foxcode.exe`（版本 `0.1.327`）。

前端构建仍有 Browserslist 数据、动态/静态混合导入及大分包提示。
本机通过直接执行 Node 包入口并补充当前进程的 `node_modules/.bin` 路径完成验证，未修改项目构建脚本。
验证覆盖上述两个后端包和前端相关用例，未运行依赖数据库/容器的集成测试；此记录不包含生产部署或真实账户计费联调。

### 后续更新方式

获取上游提交后，按功能检查实现与依赖，挑选关联修复并使用 `-x` 保留来源。
已挑选的提交会生成新的本地提交 ID，后续筛选时需要结合本文件、提交来源和 `git cherry`，不能仅凭 `HEAD..upstream/main` 重复引入。
涉及前端的功能继续使用 FoxCode 主题变量，并验证原有定制页面。
部署时使用本分支自行构建的产物或镜像，避免上游发布产物覆盖定制界面。

## 2026-09-18：实用控制台改版适配 FoxCode

根据新的授权，在 `17a73fb80` 上引入上游 `bbdd21d98` 之前的实用前端改进。
本节更新前文“暂不引入控制台改版”的选择。使用文件级移植保留本分支定制，来源为
`dbc7595d0`（密钥/用量工作区）、`92e388884`（控制台布局）、`c2fda3009`（用户仪表盘）、
`a67a9b232`（本地预览）、`f7bdc2e73`（响应式与重试修复），并采用 `803a1dd08`、
`bbdd21d98` 的相关测试与预览构建隔离方案。

### 实际合入范围

- 仪表盘：快捷入口、余额/消费/请求/密钥指标、平台明细、趋势和模型分布、最近使用；首次概览加载失败提供重试。
- 密钥页：标题与数量、筛选换行、工具按钮名称、手机列设置菜单与分页；原提供商选择和智能路由行为保持。
- 用量页：日期范围、统计与图表分区、筛选工具、导出按钮；保留成功/错误请求页签和计费字段。
- 布局只对 `/dashboard`、`/keys`、`/usage` 生效，提供面包屑、响应式顶栏和键盘可访问的余额说明。
- 四类图表支持显式动画时长；这些用户页面遵循系统减少动态效果偏好，其余调用沿用默认值。

新增样式统一映射到 `foxcode-tokens.css`，使用 FoxCode 陶土橙、米白和暖深色；
主要按钮使用独立的 `--ui-action` 与 `--ui-on-brand` 对比色，图表继续使用现有品牌调色板。
省略上游英文装饰编号，并修正手机日期弹层的 CSS 优先级，使两个日期输入框纵向排列。
网站名称继续来自后台品牌设置；仅本地演示默认显示 FoxCode。

没有改动后端、数据库迁移、依赖锁文件、公开首页、登录注册页和模型广场。
没有移入上游线上发布记录与运维脚本，后端源码版本仍为 `0.1.327`。

### 本地预览与验证

在 `frontend` 目录执行 `corepack pnpm@9.15.5 run dev:console`，访问
<http://127.0.0.1:4317/dashboard>、<http://127.0.0.1:4317/keys>、<http://127.0.0.1:4317/usage>。
预览仅监听回环地址，使用进程内演示数据，密钥增删改在重启后清空；不代理真实后端。
独立 Vite 配置禁止 build/static preview，生产构建不导入演示认证和数据。

- 全量 ESLint、应用 `vue-tsc -b`、预览工具 TypeScript 检查与 Vite 生产构建通过。
- 33 个测试文件、341 项测试通过：327 项应用测试及 14 项预览隔离/模拟 API 测试。
- 浏览器检查覆盖三页、中英文、浅深色和 320/390/768/1024/1440 像素（英文为 320/390/1440），共 48 组；无页面横向溢出、坏图、空白图表或运行异常。
- 1440/390/320 像素交互检查覆盖概览失败重试、日期选择、主题切换、手机导航、列菜单、提供商切换与分组清理、创建和筛选演示密钥、CSV 导出、错误页签以及离开控制台后的样式隔离。
- 已人工查看桌面、手机、深色仪表盘、提供商弹窗和手机日期菜单；本机截图/报告位于忽略的 `output/console-qa/`。
- `make test-frontend` 与 CI 增加控制台回归、预览隔离检查及生产构建；生产产物扫描未包含演示凭据标记。

构建保留已有 Browserslist、混合导入和较大分包提示。上述浏览器验证使用本地演示接口，
未进行真实账户/计费联调，也未部署服务器。仓库内原有三个 `analysis/*-preview.py` / `analysis/*-visual-check.py`
删除状态属于此前工作，本次没有恢复或纳入提交。

## 2026-09-19：同步上游 0.1.331

从本地 `c405721e8` 出发，以此前已核对的上游 `bbdd21d98` 为基线，按文件三方合并
`bbdd21d98..cf9ac5e3d695a157855325d22e3494c21b4f16a5` 的应用代码。
完成时再次查询上游 main，目标仍为 `cf9ac5e3d`。后端（含版本号）与该目标树完全一致；
前端采用其功能实现，并保留本分支的 FoxCode 适配。后续更新应从此上游目标继续比较。

### 合入内容

- `7503391b8`：Grok 异步视频保存实际路由分组，状态查询和视频读取沿用该分组结算，校验归属并处理旧任务的歧义。
- `074a03f12`、`9a23fbfe0`、`6f75eadfc`、`3bfb276d2`、`32f6aff03`、`1d2985acb`、`f8677033b`、`3769ff527`：暂停账户的 OAuth 刷新、严格 Chat 上游角色兼容、DeepSeek 工具图片及并行结果顺序、取消请求后的亲和性、manifest 校验、Gemini 映射模型发现和 Antigravity 系统前缀清理。
- `ea824dcea`、`b40e3c7cf`、`50cbaaa06`：用量汇总索引范围、事务一致性与并发清理回归。
- `6bea130b3`、`66b96b83b`：Codex ticket 基础能力、最多 8 个并发采集任务、实时禁用、代理设置与敏感字段隐藏；功能默认关闭。
- `831826cd7`、`c55895965`：无限画布进入时仅查找和复用可用密钥；缺少时由“创建并进入”显式创建，加入并发去重、幂等请求及账户切换检查；停用、过期或额度耗尽时提示到密钥管理处理。
- `3aa5643a5`、`5eb5a1ec4`、`4d17b6ad3`：兑换历史分页兼容、支付配置请求复用、自定义金额、订阅状态、公告部分失败、剪贴板、TOTP、对话框和分页等修复及测试。
- 同步范围内的仪表盘简化、可取消统计快照、按需展开平台/最近使用、管理导航搜索、账户/分组/用户/用量筛选及支付、订阅、订单、个人资料、兑换、返利、白名单和渠道页面布局。
- 扩展本地演示接口和管理员预览；Makefile 合入新增关键回归，CI 增加 ticket race 检查并保留演示产物隔离检查。

### FoxCode 适配

保留公开首页、登录页和模型广场的定制设计，密钥提供商选择继续使用现有实现。
注册页仅合入优惠码开关首次渲染修复。控制台新增样式仍映射到 `foxcode-tokens.css`，
保留暖色侧栏选中态、图表品牌色、深色按钮对比度、动态主题响应及手机日期弹层修复；
支付结果页也采用相同颜色变量，移除新增页面的装饰性英文编号。
修复 320 像素英文模式下分组操作按钮和图表筛选栏溢出，允许换行，表格仍可内部滚动。

未合入上游生产 rollout 脚本、特定主机发布流水线和部署审计历史；它们不代表本仓库部署状态。
没有新增数据库迁移或依赖锁文件变更。原有三个 analysis 脚本删除状态仍未纳入本次提交。

### 本地验证及边界

- 全量 ESLint、应用和演示工具 TypeScript 检查通过；303 个应用测试文件、2,284 项测试，以及 28 项预览接口/隔离测试通过。
- 后续主题和响应式调整对应的仪表盘、支付、图表、分组及管理用量回归通过。
- Go 1.26.6 的 `go test -tags=unit ./...` 除 repository 的 3 项 shell 测试外通过；补充本机 Git `bin` 到当前进程 PATH 后，完整 repository 包重跑通过，全部包均完成验证。
- Vite 生产构建和 Go `-tags=embed` 构建通过，本机程序为 `backend/bin/sub2api-foxcode.exe`，版本 `0.1.331`；生产产物不包含预览凭据标记。
- 原有三页在中英文、浅深色、320/390/768/1024/1440 像素下共 48 组显示检查通过；新增 15 个页面在桌面中文浅色、手机中文深色和 320 像素英文浅色下共 45 组通过，无页面横向溢出、坏图或页面运行异常。
- 浏览器交互验证了失败重试、日期/主题/导航、提供商切换与密钥创建筛选、CSV 导出、账户筛选重置和导航搜索；画布验证显式创建、幂等请求头、复用和停用提示。画布 iframe 使用本地占位响应，未验证真实绘图服务。
- 截图和报告位于忽略的 `output/console-qa/`、`output/sync331-qa/`。管理员设置页部分演示接口尚未覆盖，因此不计入 45 组通过结果；设置功能由自动化测试验证。

本机没有可用的 Docker/C 编译器，未运行 PostgreSQL/Redis 容器集成测试和本地 race 检查；CI 保留这些检查。
构建仍有既有 Browserslist、混合导入和大分包提示。没有部署生产服务器或进行真实账户、支付、Grok 视频计费联调。
无限画布应用仍需服务器提供 `/canvas/`，本次密钥流程修复不等于已确认此前线上通用报错的具体原因。

本地预览：在 `frontend` 下运行 `corepack pnpm@9.15.5 run dev:console`，访问
<http://127.0.0.1:4317/__preview/user> 或 <http://127.0.0.1:4317/__preview/admin> 切换角色。
预览使用内存演示数据，不连接真实后端，重启会清空操作。
